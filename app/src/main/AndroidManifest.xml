<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akshant">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Akshant">
        <activity
            android:name=".CropProtection"
            android:exported="true" />
        <activity
            android:name=".TrackCropGrowth"
            android:exported="true" />
        <activity
            android:name=".Fertilizers"
            android:exported="true" />
        <activity
            android:name=".IrrigationInfo"
            android:exported="true" />
        <activity
            android:name=".FieldPreparation"
            android:exported="true" />
        <activity
            android:name=".Seed"
            android:exported="true" />
        <activity
            android:name=".AgricultureIntelligence3"
            android:exported="true" />
        <activity
            android:name=".PropertyValue"
            android:exported="true" />
        <activity
            android:name=".AgricultureIntelligence"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ChooseCrop"
            android:exported="true" />
        <activity
            android:name=".ListView"
            android:exported="true" />
        <activity android:name=".OwnerList" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDKymeBXNeiFWY5jRUejv6zItpmr2MVyQ0" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".PropertyDetails" />
        <activity android:name=".LocateProperty" />
        <activity android:name=".ConfirmDetails" />
        <activity android:name=".BankDetails" />
        <activity android:name=".ChoosePropertyType" />
        <activity android:name=".otpSubmitHindi" />
        <activity android:name=".optSubmitEnglish" />
        <activity android:name=".loginHindi" />
        <activity android:name=".loginEnglish" />
        <activity android:name=".homePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivityHindi" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>